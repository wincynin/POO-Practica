package es.upm.etsisi.poo.domain.ticket;

// Represents a state of a ticket in the system, can be EMPTY, ACTIVE, or CLOSED as defined by E2.
public enum TicketState {
    EMPTY,
    ACTIVE,
    CLOSED
}